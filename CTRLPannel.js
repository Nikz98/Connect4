let logTracker = true,      // short strings to track movement of code
    logDebug = true,        // variables or information to ensure code is working correctly
    logFunction = true,     // the function that is running
    logBoard = true         // current board array state

function printLog(item, string) {
    if (item) {console.log(string)}
}